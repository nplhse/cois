# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    app.cookie_consent: '%env(bool:default:app.default.cookie_consent:APP_COOKIE_CONSENT)%'
    app.locale: "%env(string:default:app.default.locale:APP_LOCALE)%"
    app.supported_locales: "en|de"
    app.mailer.admin: '%env(string:default:app.default.mailer.admin:APP_MAILER_ADMIN)%'
    app.mailer.from: '%env(string:default:app.default.mailer.from:APP_MAILER_FROM)%'
    app.mailer.sender: '%env(string:default:app.default.mailer.sender:APP_MAILER_SENDER)%'
    app.registration: '%env(bool:default:app.default.registration:APP_REGISTRATION)%'
    app.terms: '%env(bool:default:app.default.terms:APP_TERMS)%'
    app.title: '%env(string:default:app.default.title:APP_TITLE)%'

    app.default.cookie_consent: true
    app.default.locale: 'en'
    app.default.mailer.admin: 'admin@cois.dev'
    app.default.mailer.from: 'Collaborative IVENA statistics'
    app.default.mailer.sender: 'noreply@cois.dev'
    app.default.registration: true
    app.default.terms: true
    app.default.title: 'Collaborative IVENA statistics'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            $appCookieConsent: "%app.cookie_consent%"
            $appLocale: "%app.locale%"
            $appMailerSender: "%app.mailer.sender%"
            $appMailerFrom: "%app.mailer.from%"
            $appRegistration: "%app.registration%"
            $appTerms: "%app.terms%"
            $appTitle: "%app.title%"
            $projectDir: '%kernel.project_dir%'

    _instanceof:
        App\Application\Contract\HandlerInterface:
            public: true
            tags: ["messenger.message_handler"]

        App\Application\Contract\FilterInterface:
            tags: ["app.filters"]

        App\Application\Contract\ImportWriterInterface:
            tags: [ "app.import_writer" ]

        App\Application\Contract\AllocationImportWriterInterface:
            tags: [ "app.allocation_import_writer" ]

        App\Application\Contract\ImportReaderInterface:
            tags: [ "app.import_reader" ]

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: "../src/"
        exclude:
            - "../src/DependencyInjection/"
            - "../src/Entity/"
            - "../src/Kernel.php"
            - "../src/Tests/"

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: "../src/Controller/"
        tags: ["controller.service_arguments"]

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\Doctrine\:
        resource: '../src/Doctrine/'
        tags: ['doctrine.orm.entity_listener']

    Symfony\Bridge\Doctrine\Security\RememberMe\DoctrineTokenProvider: ~

    App\Service\FilterService:
        class: 'App\Service\FilterService'
        arguments:
            - !tagged_iterator { tag: "app.filters", default_index_method: "getParam" }

    App\Service\Import\ImportService:
        class: 'App\Service\Import\ImportService'
        arguments:
            - '@doctrine.orm.entity_manager'
            - '@debug.stopwatch'
            - !tagged_iterator { tag: "app.import_reader", default_index_method: "getFileType" }
            - !tagged_iterator { tag: "app.import_writer", default_priority_method: "getPriority" }

    App\Service\Import\Writer\AllocationImportWriter:
        class: 'App\Service\Import\Writer\AllocationImportWriter'
        arguments:
            - !tagged_iterator { tag: "app.allocation_import_writer" }
            - '@validator'
