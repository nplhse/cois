{{ form_start(form) }}

    {{ form_row(form.name) }}

    {{ form_row(form.supplyArea) }}

    {{ form_row(form.address) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
