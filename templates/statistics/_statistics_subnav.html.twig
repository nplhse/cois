<h3>
    {{ 'General'|trans }}
</h3>
<ul class="list-group mb-3">
    {% if
        app.request.attributes.get('_route')
            starts with 'app_statistics_gender' %}
        <li class="list-group-item active">
            <a class="text-light" href="{{ path('app_statistics_gender') }}">
                <i class="bi bi-gender-ambiguous"></i> {{ 'Gender'|trans }}
            </a>
        </li>
    {% else %}
        <li class="list-group-item">
            <a href="{{ path('app_statistics_gender') }}">
                <i class="bi bi-gender-ambiguous"></i> {{ 'Gender'|trans }}
            </a>
        </li>
    {% endif %}
    {% if
        app.request.attributes.get('_route')
            starts with 'app_statistics_time' %}
        <li class="list-group-item active">
            <a class="text-light" href="{{ path('app_statistics_times') }}">
                <i class="bi bi-clock-fill"></i> {{ 'Times'|trans }}
            </a>
        </li>
    {% else %}
        <li class="list-group-item">
            <a href="{{ path('app_statistics_times') }}">
                <i class="bi bi-clock"></i> {{ 'Times'|trans }}
            </a>
        </li>
    {% endif %}
</ul>
