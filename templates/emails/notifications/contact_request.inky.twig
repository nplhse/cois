{% extends '@email/default/notifications/body.html.twig' %}

{% block content %}
    <row>
        {{
            'notifications.contact.request'|trans(
                {
                    '%name%': name,
                    '%email_address%': email_address,
                    '%subject%': subject
                },
                'emails'
            )
        }}
    </row>

    <spacer size="25"></spacer>

    <row>
        {{ message }}
    </row>
{% endblock %}
