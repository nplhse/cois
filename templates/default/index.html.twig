{% extends 'base.html.twig' %}

{% block title %}Welcome - {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <h1>Welcome to COIS - Collaborative IVENA statistics</h1>

        <div class="col-md">
            <p>Want to know where to get started? Here are some ideas.</p>

            <div class="card mb-3">
                <div class="card-body">
                    <h2 class="card-title">Allocations</h2>
                    <p class="card-text">Browse trough all the data, filter it and explore what IVENA got for us.</p>
                    <a href="{{ path("allocation_index") }}" class="btn btn-primary">View all Allocations</a>
                </div>
            </div>

            <div class="card mb-3">
                <div class="card-body">
                    <h2 class="card-title">Your Hospital</h2>
                    <p class="card-text">Check if your hospital has been set up correctly and change data if necessary.</p>
                    <a href="{{ path("hospital_edit") }}" class="btn btn-primary">Edit hospital</a>
                    <a href="{{ path("hospital_index") }}" class="btn btn-secondary">View all hospitals</a>
                </div>
            </div>

            <div class="card mb-3">
                <div class="card-body">
                    <h2 class="card-title">Import data</h2>
                    <p class="card-text">Add some of your data to the database.</p>
                    <a href="{{ path("import_index") }}" class="btn btn-primary">Import data</a>
                </div>
            </div>
        </div>
        <div class="col-md">
            <div class="card mb-3">
                <div class="card-header">
                    Statistics
                </div>
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p>There are currently <strong>{{ allocations }}</strong> Allocations, from <strong>{{ hospitals }}</strong> hospitals.</p>
                        <footer class="blockquote-footer">With <strong>{{ hospital_allocations }}</strong> Allocations from your hospital</footer>
                    </blockquote>
                </div>
            </div>


            <div class="card border-warning mb-3">
                <div class="card-header">
                    Changelog
                </div>
                <div class="card-body">
                    <h5 class="card-title">27.03.2021</h5>
                    <ul>
                        <li>Added first filter to search by (at least some) basic fields of the Allocations.</li>
                    </ul>
                    <h5 class="card-title">18.03.2021</h5>
                    <ul>
                        <li>Added first filter to sort by (at least some) fields of the Allocations.</li>
                    </ul>
                    <h5 class="card-title">10.03.2021</h5>
                    <ul>
                        <li>Improved imports with a better layout and a basic detail view.</li>
                    </ul>
                    <h5 class="card-title">08.03.2021</h5>
                    <ul>
                        <li>Small improvements e.g. fixed typos& namings, set correct timezone, improved imports.</li>
                    </ul>
                    <h5 class="card-title">03.03.2021</h5>
                    <ul>
                        <li>First Alpha-Release.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
