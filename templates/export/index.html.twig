{% extends 'base.html.twig' %}

{% import '_elements/macro/breadcrumb.html.twig' as breadcrumbs %}

{% block title %}
    {{ 'Export'|trans }} - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            {{
                breadcrumbs.render(
                    [
                        {
                            url: url('app_dashboard'),
                            label: 'Home'|trans
                        },
                        {
                            label: 'Export'|trans
                        }
                    ]
                )
            }}
        </div>
    </div>

    <div class="row">
        <div class="col">
            <h1>
                {{ 'Exports'|trans }}
            </h1>

            <table class="table table-hover">
                <thead>
                    <th class="w-25">
                        {{ 'Category'|trans }}
                    </th>
                    <th class="w-50">
                        {{ 'Name'|trans }}
                    </th>
                    <th class="w-25">
                        {{ 'Actions'|trans }}
                    </th>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            <span class="badge bg-primary">
                                <i class="bi bi-tag"></i>
                                {{ 'Demo'|trans }}
                            </span>
                        </td>
                        <td>
                            <p>
                                <a href="{{ path('app_export_c19') }}">
                                    {{ 'COVID19 demo'|trans }}
                                </a>
                                <span class="badge bg-secondary">public</span>
                            </p>
                        </td>
                        <td>
                            <a class="btn btn-primary"
                                href="{{ path('app_export_c19') }}">
                                <i class="bi bi-search"></i>
                                {{ 'View Details'|trans }}
                            </a>
                            <a class="btn btn-outline-secondary"
                                href="{{ path('app_export_c19_file') }}">
                                <i class="bi bi-download"></i>
                                {{ 'Download'|trans }}
                            </a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
